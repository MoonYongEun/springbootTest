<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
 <mapper namespace="index.dao.IndexDAO">
 	<insert id="insert" parameterType="java.util.Map">
 		insert into teststs values(select case count(*) when 0 then 1 else max(seq) + 1 end, #{name},#{kor},#{math},#{eng}) 
 	</insert>
 </mapper>